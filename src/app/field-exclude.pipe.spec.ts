import { FieldExcludePipe } from './field-exclude.pipe';

describe('FieldExcludePipe', () => {
  it('create an instance', () => {
    const pipe = new FieldExcludePipe();
    expect(pipe).toBeTruthy();
  });
});
